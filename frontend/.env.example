# Vercel / production: backend origin (no trailing slash)
# VITE_API_BASE_URL=https://hook-mining-engine.onrender.com

# Local: omit this file or leave unset — Vite proxies /api → http://localhost:8000
